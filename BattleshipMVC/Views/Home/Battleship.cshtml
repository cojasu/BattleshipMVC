@model BattleshipMVC.Models.Battleship

        @using (Html.BeginForm())
        { 
            <table>
                @for (int i = 1; i < 11; i++)
                {
                    <tr>
                       @for (int j = 1; j < 11; j++)
                       {
                            <td><input type="button" name="button" value="@i @j" /></td>
                       }
                    </tr>
                }
            </table>
        }